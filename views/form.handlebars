

<div id="form-container">
    <form id="question-form">
        <h2>Multiple-Choice Questionnaire</h2>

        {{#each questions}}
        <div class="question">
            <p>{{this.questionText}}</p>
            {{#each this.choices}}
            <label>
                <input type="radio" name="{{../questionId}}" value="{{@index}}"> {{this}}
            </label>
            {{/each}}
        </div>
        {{/each}}

        <button type="submit">Submit</button>
    </form>
</div>

<div id="form-template" type="text/x-handlebars-template">
    <form id="question-form">
        <h2>Multiple-Choice Questionnaire</h2>

        {{#each questions}}
        <div class="question">
            <p>{{this.questionText}}</p>
            {{#each this.choices}}
            <label>
                <input type="radio" name="{{../questionId}}" value="{{@index}}"> {{this}}
            </label>
            {{/each}}
        </div>
        {{/each}}

        <button class= "formButton" type="submit">Submit</button>
    </form>
</div>

<div>
    // Sample data for questions
    var data = {
        questions: [
            {
                questionId: 1,
                questionText: "What's your preferred role in a group?",
                choices: ["Lead the charge and smash things", "Charm and inspire others", "Heal and support your allies", "Connect with nature and transform", "Swear an oath to protect the innocent", "Unleash powerful magic with your will", "Make a pact with a mysterious entity", "Study and manipulate arcane knowledge"]
            },
            {
                questionId: 2,
                questionText: "Question 2: What is your favorite animal?",
                choices: ["Dog", "Cat", "Lion", "Elephant", "Dolphin", "Hawk", "Snake", "Tiger", "Horse"]
            },
            {
                questionId: 3,
                questionText: "Question 3: What is your favorite food?",
                choices: ["Pizza", "Burger", "Sushi", "Pasta", "Steak", "Tacos", "Salad", "Ice Cream", "Curry"]
            },
            {
                questionId: 4,
                questionText: "Question 4: What is your favorite movie genre?",
                choices: ["Action", "Comedy", "Drama", "Horror", "Science Fiction", "Fantasy", "Romance", "Adventure", "Animation"]
            },
            {
                questionId: 5,
                questionText: "Question 5: What is your favorite season?",
                choices: ["Spring", "Summer", "Fall", "Winter", "I don't have a favorite season"]
            },
            {
                questionId: 6,
                questionText: "Question 6: What is your favorite hobby?",
                choices: ["Reading", "Gaming", "Cooking", "Hiking", "Painting", "Music", "Photography", "Sports", "Other"]
            }
        ]
    };

    // Compile the Handlebars template
    var source = $("#form-template").html();
    var template = Handlebars.compile(source);

    var html = template(data);

    $("#form-container").html(html);

    $("#question-form").on("submit", function (event) {
        event.preventDefault();
    });
</div>
<script>src="/js/form.js"</script>
